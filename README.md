# HTML, streamlit, fastapi

# docker compose
```
git pull ...

docker-compose up -d
```

# test
```
# web
curl http://localhost:8080

# streamlit
curl http://localhost:8180

# fastapi
curl http://localhost:8280

```